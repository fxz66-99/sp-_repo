spring:
  application:
    name: order-service
    
# server:
#  port: 8201
  
  
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka
      
feign:
  hystrix:
    enabled: true  #启用feign
    
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream  #暴露端点
        
---
spring:
  profiles: order1
  
server:
  port: 8201
  
---
spring:
  profiles: order2
  
server:
  port: 8202